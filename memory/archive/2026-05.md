# waoowaoo Memory Archive — 2026-05

## [2026-05-27 | Production Prep workspace MVP 完成]
- **Events**：在 production-bible foundation 之上完成项目级“前期设定 / Production Bible”工作台；选择独立项目级真值源，而不是把 workflow run artifact 或 `NovelPromotionProject` 当长期保存边界。
- **Changes**：新增 `ProjectProductionPrep` Prisma model/migration、`src/lib/production-prep/**` service/store/generation/merge、production-prep 三组项目 API、React Query hooks、`/workspace/[projectId]/production-prep` 页面、项目详情入口、中英文文案和定向 unit/API contract 测试。
- **Insights**：锁定策略必须在 service merge 层统一表达，自动生成只更新未锁定的 characters/locations/props/style/continuity，并通过 conflicts 显式报告被保留的锁定项；真实 extract/plan 继续依赖项目 `analysisModel`，测试不调用 provider。

## [2026-05-27 | visual workflow builder child #8 完成]
- **Events**：完成 Coze-like visual workflow builder 当前 epic 的 child #8 Business node runtime Activities；重新参考 React Flow、Temporal、Dify、n8n 的官方资料后，继续确认 `WorkflowCanvasDefinition DSL -> node catalog/config schema -> validator/compiler -> immutable published versions -> execution planner -> Temporal published runtime Activities` 是当前最稳的可维护架构。
- **Changes**：published workflow runtime 已支持 `runtime.smoke`、`input.user`、`data.transform`、`artifact.persist`、`llm.transform`、`llm.analysis` 和 `media.generate(image|video|audio)`。audio 作为独立 TTS handler 接入 `audioModel`、`audioVoice`、`audioRate`、`audioMaxFreezeSeconds`、`generateAudio`、`withVoiceBilling`、`processMediaResult(type='audio')`、`workflow.media.result` 和 `workflow.media.external-job`。
- **Insights**：React Flow 只适合作为节点/边/viewport 交互层，不应成为后端真值；Temporal Workflow 必须保持 deterministic，外部 API、DB、LLM、storage、billing 必须留在 Activities；media image/video/audio 共用 media cache/checkpoint，但 provider/model/source/billing/storage 语义必须分 handler 隔离。

## [2026-05-27 | visual workflow builder epic final validation]
- **Events**：完成 final audit，补齐 workflow builder 执行后的 run/step 可观察状态。当前 epic 的 `SUBTASKS.csv` #1-#9 均为 `DONE`，`EPIC.md` Done-When 已勾选。
- **Changes**：新增 `usePublishedWorkflowRunStream` 和 `WorkflowRunStatePanel`；shared run-stream executor 现在能观察非 task 的 published workflow queued/running `runId` 响应，并继续通过 `/api/runs/:runId/events` 读取 lifecycle events。
- **Insights**：builder 不应自己维护运行状态；执行状态必须复用 run-runtime read model 和 shared run-stream state machine。只显示 runId 不能满足可视化工作流验收，必须至少展示 run 状态、step 顺序、step 状态、错误和输出摘要。

## [2026-05-27 | production bible foundation 完成]
- **Events**：按“小说/手动提示词 -> 制片准备资产 -> 分集/分场/分镜 -> 媒体生成”的目标，选择 contract-first production-prep foundation，而不是把既有 novel-promotion worker 直接绑定成唯一流程。
- **Changes**：新增 `src/lib/production-bible/` 严格 schema、语义校验、production 节点输出解析和提示词构造；新增 `story.extractBible`、`story.planEpisodes`、`scene.breakdown`、`shot.plan`、`human.review` 节点；published workflow runtime 通过现有 LLM Activity/model/billing/cache 边界执行 production 节点，并严格解析输出。新增 `docs/production-bible-workflow-architecture.md`。
- **Insights**：自动小说解析和人工手动刻画必须收敛到同一套 `ProductionPrepDocument` / asset cards / episode / scene / shot schema；`human.review` 当前只记录 checkpoint，不伪造审批，后续真正暂停/恢复应接 run-runtime/Temporal signal。

## [2026-05-27 | compat cleanup]
- **Events**：按“开发阶段无历史包袱”的要求，审查 production-bible / published workflow runtime 的旧入口、兼容壳和重复依赖读取逻辑；保留仍承担当前迁移路径的 BullMQ / run-runtime strangler 边界。
- **Changes**：删除无调用的 `isProductionWorkflowNodeType`、`PRODUCTION_NODE_TYPES`、`isTemporalPublishedWorkflowNodeSupported` 和 `src/lib/workflow-engine/node-types.ts` 转发壳；新增 `src/lib/workflow-runtime/temporal/published-workflow-step-context.ts` 统一直接依赖读取。
- **Insights**：planner support 和 Activity dispatch 继续从同一 executor map 派生；直接依赖缺失错误现在由 shared helper 统一抛出，artifact.persist 的“至少一个依赖”约束仍留在 artifact 边界。
